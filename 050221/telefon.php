<?php
$str=$_POST["name"];
$regexp="/^((80)|(\+375))?[-]?[\(](212)?[\)][-]?\d{2}[-]?\d{2}[-]?\d{2}$/iu";
    if (preg_match($regexp,$str)) {
        echo "Введен правильный номер";
    } else {
        echo "Введен неправильный номер";
    }
